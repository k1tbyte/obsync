{
	"name": "obsync-relay",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "partykit dev",
		"deploy": "partykit deploy",
		"typecheck": "tsc -noEmit",
		"test": "vitest run --passWithNoTests"
	},
	"devDependencies": {
		"partykit": "^0.0.111",
		"vitest": "^4.1.11"
	}
}
