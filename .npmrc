tag-version-prefix="v"
git-tag-version=false
commit-hooks=false

# wrangler and partykit resolve their runtimes against a flat node_modules.
# pnpm's default symlinked layout breaks them, so use a hoisted node_modules.
node-linker=hoisted