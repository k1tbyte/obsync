tag-version-prefix="v"
git-tag-version=false
commit-hooks=false

# wrangler resolves its runtime against a flat node_modules.
# pnpm's default symlinked layout breaks it, so use a hoisted node_modules.
node-linker=hoisted